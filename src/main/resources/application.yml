spring:
  datasource:
    # 测试用 H2 内存数据库；生产环境改为 SQL Server 配置
    url: jdbc:h2:mem:campus_ai;MODE=MSSQLServer;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 123456
    driver-class-name: org.h2.Driver
    # SQL Server 配置（生产环境启用）
    # url: jdbc:sqlserver://localhost:1433;databaseName=campus_ai;encrypt=false;trustServerCertificate=true
    # username: sa
    # password: 123456
    # driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:your-dashscope-api-key-here}

server:
  port: 8088